# Prometheus Operator ServiceMonitors (app scrape targets).
# Requires the ServiceMonitor CRD. Not included on gke-dev (GCP Cloud Ops).
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - servicemonitor-product-catalog.yaml
  - servicemonitor-balancereader.yaml
