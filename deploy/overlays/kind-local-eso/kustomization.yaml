# Kind + sidecar + GHCR — secrets from Vault via ESO (not plain Secrets in git).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: banking-platform

resources:
  - ../kind-local

components:
  - ../../components/use-eso-vault
